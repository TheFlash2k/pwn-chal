service chall
{
    disable         = no
    type            = UNLISTED
    socket_type     = stream
    protocol        = tcp
    user            = RUN_AS
    wait            = no
    bind            = 0.0.0.0
    server          = /app/CHAL_NAME
    port            = PORT
}